const mdLinks = require('../index.js');


describe('mdLinks', () => {

  it('should...', () => {
    console.log('FIX ME!');
  });

});
it('devolver promesa', () => {
  expect(mdLinks()).toBe(typeof Promise);
});

});